/**
 * @mjrc/rulesets — the faan catalogue, the payment tables and the assembled
 * presets. DESIGN.md §4: rulesets are data, not code, so everything a house
 * can disagree about lives behind this barrel and nothing behind it imports
 * the engine's reducer.
 */
export * from "./patterns.js";
export * from "./payment.js";
export * from "./presets.js";
