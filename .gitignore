node_modules/
dist/
*.tsbuildinfo

.DS_Store
tools/**/*.mjs
tools/sim/logs/

# Moodboard images — local-only reference material, keep binaries out of git
moodboard/*
!moodboard/*.md

# Per-cycle training archives — 60MB of raw run dumps, regenerated by the
# harness and summarised into the committed dials-history.js / series-history.js.
tools/sim/runs/

# Local profiler dumps
*.cpuprofile

# built by tools/publish-demo.sh; the source is client/game
deploy/
.dev.vars

# gamepvp build products and local wrangler state
gamepvp/assets/
gamepvp/.wrangler/
